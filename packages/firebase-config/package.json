{
  "name": "@ecologen/firebase-config",
  "version": "1.0.0",
  "private": true,
  "description": "Firebase configuration for EcoLogen with Australian compliance",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "firebase": "^12.0.0",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}